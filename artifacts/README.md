# 本地大型产物

此目录用于数据集、embedding、Semantic ID、checkpoint 和完整预测文件。除本说明外，内容默认被 Git 忽略。

计划结构：

```text
artifacts/
├─ datasets/
├─ embeddings/
├─ semantic_ids/
├─ checkpoints/
└─ predictions/
```

任何下载或生成操作都必须在对应步骤获得确认后执行。
